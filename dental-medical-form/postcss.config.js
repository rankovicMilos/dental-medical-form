export default {
  plugins: {
    // keep this if you want PostCSS to process Tailwind directly
    autoprefixer: {}, // adds vendor prefixes
    // 'postcss-nested': {}, // example if you want nesting support
  },
};
